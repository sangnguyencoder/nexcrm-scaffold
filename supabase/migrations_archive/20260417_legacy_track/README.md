# Archived migrations (legacy / transition)

Các file trong thư mục này được giữ lại để tham chiếu lịch sử.

Quy tắc hiện tại:
- KHÔNG chạy các file này cho setup mới.
- Setup mới chỉ chạy 2 file trong `supabase/migrations`:
  1. `20260417090000_canonical_baseline.sql`
  2. `20260417090100_ops_scheduler_optional.sql` (tùy chọn)
